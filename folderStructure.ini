trendforge/
│
├── src/
│   ├── config/                # Config files (DB, JWT secrets, env vars)
│   │   └── db.js              # PostgreSQL pool setup
│   │   └── config.js          # Other configs (like JWT secret)
│   │
│   ├── controllers/           # Route handlers / business logic
│   │   └── authController.js  # Login, register, token refresh logic
│   │
│   ├── middleware/            # Express middleware (auth, error handlers)
│   │   └── authMiddleware.js  # JWT token verification
│   │   └── errorHandler.js    # Central error handling
│   │
│   ├── models/                # DB queries and schema models
│   │   └── userModel.js       # User-related DB queries
│   │
│   ├── routes/                # Express routes
│   │   └── authRoutes.js      # Routes for auth endpoints
│   │
│   ├── utils/                 # Utility functions (e.g., token generation)
│   │   └── tokenUtils.js
│   │
│   ├── app.js                 # Express app setup and middleware registration
│   └── server.js              # Server start script
│
├── migrations/                # DB migration scripts (if using tools like knex or node-pg-migrate)
│
├── tests/                    # Unit & integration tests
│
├── .env                      # Environment variables (not committed)
├── .gitignore
├── package.json
├── README.md
└── yarn.lock / package-lock.json
