my-app/
├── app/                       # App router pages and layouts
│   ├── layout.tsx            # Root layout (includes <Header /> / <Toaster /> / <ThemeProvider />)
│   ├── page.tsx              # Root page (landing page or redirect)
│   ├── login/                # Public login page
│   │   └── page.tsx
│   ├── dashboard/            # Authenticated area
│   │   └── page.tsx
│   ├── settings/             # User settings
│   │   └── page.tsx
│   └── not-found.tsx        # 404 fallback
│
├── components/               # All shared, reusable components
│   ├── ui/                   # shadcn-ui components (auto-managed)
│   ├── layout/               # Header, Sidebar, Footer, etc.
│   ├── elements/             # Tiny pieces like Avatar, Badge, Spinner
│   └── cards/                # Custom dashboard or data cards
│
├── hooks/                    # Reusable React hooks (e.g. useDarkMode)
│   └── use-toast.ts          # Toast utility from shadcn
│
├── lib/                      # Helpers (e.g. auth utils, localStorage)
│   └── utils.ts
│
├── styles/                   # Tailwind + any extra CSS
│   └── globals.css
│
├── public/                   # Static assets (e.g. logo, favicon)
│   └── logo.svg
│
├── types/                    # Global TS types (User, TokenPayload, etc.)
│   └── index.d.ts
│
├── tailwind.config.ts        # Tailwind config
├── postcss.config.js         # PostCSS config
├── tsconfig.json             # TypeScript config
└── next.config.js            # Next.js config
